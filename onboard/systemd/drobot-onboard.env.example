# Optional overrides loaded by drobot-onboard.service.
DROBOT_SERIAL_PORT=auto
DROBOT_HTTP_BIND=0.0.0.0
DROBOT_HTTP_PORT=8080

# Leave empty for a random token embedded into each served browser page.
# Set at least 16 characters when external scripts need a stable API token.
DROBOT_CONTROL_TOKEN=
